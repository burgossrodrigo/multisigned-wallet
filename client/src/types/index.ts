export type addressesType = {
    balance: string | undefined
    addressStatus: boolean | undefined
    remainingValidations: string | undefined
  }

export type valuesType = {
    amount: string
    address: string
  }